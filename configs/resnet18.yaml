model:
  type: resnet

training:
  epochs: 10
  batch_size: 64
  learning_rate: 0.001

augmentation:
  enabled: true  

  horizontal_flip:
    enabled: true
    p: 0.5

  random_crop:
    enabled: true 
    padding: 4

  rotation:
    enabled: true 
    degrees: 15